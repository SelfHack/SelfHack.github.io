name: test_env_var

on:
  schedule:
    - cron: '44 20 * * *'  #scheduled for midnight (EST)
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "Token: $USER_TOKEN"
        env:
          USER_TOKEN: ${{ secrets.USER_TOKEN }}
